{
  "name": "src-js",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "generate-test-log": "bun scripts/generate-log.js 3000 > tests/test_session_tickets.log",
    "build-demo": "bun scripts/build-demo.js",
    "clean": "bun -e \"import { rmSync } from 'fs'; try { rmSync('dist', { recursive: true, force: true }); } catch (e) {}\""
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}